version: hoyan/v1

snapshots:
  current:
    lab: labs/base-wan

scenarios:
  normal:
    snapshot: current

intents:
  - name: missing-prefix-on-bj-edge1
    scenario: normal
    rcl:
      rib_eval:
        where:
          device: bj-edge1
          prefix: 203.0.113.0/24
        aggregate: count()
        gte: 1
