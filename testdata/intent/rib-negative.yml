version: hoyan/v1

snapshots:
  current:
    lab: labs/base-wan

scenarios:
  normal:
    snapshot: current

intents:
  - name: missing-prefix-on-bj-edge1
    check:
      table: rib
      scenario: normal
      where:
        device: bj-edge1
        prefix: 203.0.113.0/24
    assert:
      exists: true
