version: hoyan/v1

snapshots:
  current:
    lab: labs/base-wan

scenarios:
  normal:
    snapshot: current

intents:
  - name: bj-http-incorrectly-expected-reachable
    scenario: normal
    rcl:
      packet_reachable:
        from: cust-bj
        to: 10.4.1.10
        protocol: tcp
        dst_port: 80
        expect: true
