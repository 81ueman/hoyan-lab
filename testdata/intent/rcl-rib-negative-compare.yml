version: hoyan/v1

vars:
  changed_prefix: 10.4.0.0/16

snapshots:
  pre:
    lab: labs/intent-change-before
  post:
    lab: labs/intent-change-after-negative

scenarios:
  normal:
    snapshot: pre

intents:
  - name: unrelated-routes-unchanged
    rcl:
      rib_eq:
        left: pre
        right: post
        where:
          not:
            prefix: ${changed_prefix}
