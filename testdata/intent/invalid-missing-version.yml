snapshots:
  current:
    lab: labs/base-wan

scenarios:
  normal:
    snapshot: current

intents:
  - name: missing-version
    scenario: normal
    rcl:
      rib_eval:
        where:
          device: bj-edge1
        aggregate: count()
        gte: 1
