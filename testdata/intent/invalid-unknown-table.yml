version: hoyan/v1

snapshots:
  current:
    lab: labs/base-wan

scenarios:
  normal:
    snapshot: current

intents:
  - name: bad-table
    check:
      table: routes
      scenario: normal
      where:
        device: bj-edge1
    assert:
      exists: true
