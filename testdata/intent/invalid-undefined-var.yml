version: hoyan/v1

snapshots:
  current:
    lab: labs/base-wan

scenarios:
  normal:
    snapshot: current

intents:
  - name: undefined-var
    scenario: normal
    rcl:
      rib_eval:
        where:
          device: ${missing_device}
        aggregate: count()
        gte: 1
