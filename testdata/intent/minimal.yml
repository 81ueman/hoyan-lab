version: hoyan/v1

snapshots:
  current:
    lab: labs/base-wan

scenarios:
  normal:
    snapshot: current

intents:
  - name: bj-edge1-has-service-prefix
    scenario: normal
    rcl:
      rib_eval:
        where:
          device: bj-edge1
          prefix: 10.4.0.0/16
        aggregate: count()
        gte: 1
