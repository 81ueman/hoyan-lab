The code-review agent finished. You are the implementation agent for this branch.

Please triage the review output below:
- Fix every valid Must-fix item.
- If a finding is a false positive or intentionally out of scope, explain why.
- Keep the branch focused on the original `.pi-task.md`.
- Run relevant tests after any changes.
- Report:
  - classification of each review finding
  - files changed
  - how each valid finding was addressed
  - test command and result
  - whether the branch is ready for re-review

Review output:

# Code Review: `codex/device-session-abstraction`

**Review base:** `main` (HEAD at 96977b3 — merge-base)
**Commits on branch:** none (all changes are uncommitted working-tree changes across 9 files)
**Spec source:** `.pi-task.md`
**Standards source:** No explicit coding-standards file found in repo; baseline smell list (Fowler, *Refactoring* ch.3) applies.

---

## Standards

### 1. Duplicated Code — `shellQuote`/`shellJoin` (cleaned up, good)

The old `srlinuxjson/exec.go` had its own `shellQuote` and `shellJoin`. Those were removed from there and re-created in `device/device.go`. ✅ This is a net improvement — only one copy exists now.

**Status:** No issue; the duplication was removed.

### 2. Duplicated Code — `DockerSession.ExecTTY` raw command construction (minor judgement call)

`DockerSession.ExecTTY` in `device/device.go` constructs a `docker exec -it` command string directly (`command := "docker exec -it " + shellQuote(s.containerName) + " " + shellJoin(args)`) and passes it to the runner via `script -q /dev/null -c <command>`. This replicates the same pattern that was in the old `srlinuxjson/exec.go`. The old code had the same fundamental approach (concatenating docker command strings), so this is not a regression. It's reasonable for a `DockerSession` — docker is the impl detail.

**Status:** Acceptable as-is for a docker-backed session implementation.

### 3. Missing direct tests for `device` package

The `internal/adapter/live/device` package has no test files. `DockerSession.Exec`, `DockerSession.ExecTTY`, `VendorCollector.SessionForNode` are exercised indirectly through `srlinuxjson` and collector tests, but the device package itself lacks a test suite. The spec says "Update tests/fakes accordingly" — the `srlinuxjson/exec_test.go` was properly updated with `testSession`, but the device package gained new exported API (`DockerSession`, `DeviceSession`, `ExecTTY`, `SessionForNode`) without direct unit tests.

**Status:** Minor concern. Not a hard violation since integration-level tests cover the behaviour.

### 4. Speculative Generality — `ttySession` optional interface

`srlinuxjson/exec.go` defines a `ttySession` optional interface. This is a clean way to let sessions optionally provide TTY execution without bloating the base `Session` interface. Appropriate pattern; not speculative since the TTY fallback is a real requirement.

**Status:** No issue.

---

## Spec

Based on `.pi-task.md`:

| Requirement | Status |
|---|---|
| Introduce an adapter-level execution/session abstraction (`DeviceSession`, etc.) | ✅ Done — `DeviceSession` interface in `device/device.go` |
| Implement a containerlab/docker-backed session | ✅ Done — `DockerSession` in `device/device.go` |
| Refactor `DockerExecutor` usage so vendor collectors don't directly construct `docker exec` command strings | ✅ Done — all three collectors (FRR, cEOS, SR Linux) use `session.Exec()` |
| Keep public CLI/containerlab behaviour working | ✅ Tests pass (`go test ./internal/adapter/live/... ./internal/adapter/srlinuxjson/...`) |
| Avoid full SSH; provide clean seam/stub for real-device session | ✅ `DeviceSession` interface is minimal; non-docker sessions can implement it |
| Update tests/fakes accordingly | ✅ `exec_test.go` uses `testSession`; tests pass |

### 1. Error messages still reference "docker exec -i" (minor scope creep / misleading)

In the old code, error messages like `"docker exec -i %s ..."` accurately described what happened. Now that execution goes through `session.Exec()`, these messages are misleading — a future SSH-based `DeviceSession` would produce errors that falsely claim "docker exec -i". Affected lines:

- `fib/collector.go` lines 82, 85, 97, 100 (4 occurrences)
- `rib/collector.go` lines 122, 137, 145 (3 occurrences)
- `rib/route_table_collect.go` lines 22, 25, 28, 40, 83 (5 occurrences)

**Status:** The spec doesn't explicitly require fixing error messages, but the refactoring goal ("vendor collectors do not directly construct docker exec command strings") is undermined by error messages that do exactly that. **Nice-to-have** — update to `"session %s command ..."` or remove the "docker exec -i" prefix entirely.

### 2. No real-device session stub included

The spec says "provide a clean seam/stub for real-device session if appropriate." The `DeviceSession` interface provides the seam, but no stub is included. This is acceptable per the "if appropriate" qualifier — the spec deliberately leaves this optional.

**Status:** No issue; the seam is sufficient.

---

## Must-fix items before PR

1. **None identified that block PR.** All build, vet, and test checks pass. The error-message nit is a nice-to-have, not a blocker.

## Nice-to-have items

1. **Update "docker exec -i" error messages** in `fib/collector.go`, `rib/collector.go`, and `rib/route_table_collect.go` to use session-agnostic language (12 occurrences total).
2. **Add direct unit tests for `device` package** — at minimum verify `DockerSession.Exec` delegates args correctly and `SessionForNode` returns a session with the right container name.

## PR-ready assessment

**Yes, the branch is PR-ready.** The core abstraction (`DeviceSession` interface + `DockerSession` implementation) is clean, all vendor collectors have been refactored to use it, the containerlab integration point is updated, all tests pass, and the seam for future real-device sessions is in place. The two nice-to-have items (error messages, device-package tests) can be addressed in follow-up work without blocking the PR.

---

### Summary

- **Standards findings:** 1 minor (device package lacks direct tests)
- **Spec findings:** 1 nice-to-have (error messages mention docker exec)
- **Worst per axis:**
  - Standards — device package missing its own test file for new exported API
  - Spec — error messages leak docker implementation detail
