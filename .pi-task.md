# Task: introduce device execution/session abstraction for live collectors

You are implementing in this worktree only. Do not commit, push, create PRs, or merge.

Goal: decouple vendor live RIB/FIB collectors from hard-coded `docker exec` so the same vendor collectors can later run against containerlab or real devices.

Required outcomes:
- Introduce an adapter-level execution/session abstraction (e.g. `DeviceSession`, `CLISession`, or equivalent) that represents running commands on a network device target.
- Implement a containerlab/docker-backed session that preserves existing behavior.
- Refactor `internal/adapter/live/device.DockerExecutor` usage so vendor collectors do not directly construct `docker exec` command strings.
- Keep public CLI/containerlab behavior working.
- Avoid implementing full SSH unless small; provide a clean seam/stub for real-device session if appropriate.
- Update tests/fakes accordingly.
- Run relevant tests, preferably `go test ./internal/adapter/live/... ./internal/adapter/cli` and wider tests if feasible.

Context from analysis:
- `internal/adapter/live/device/device.go` has `DockerExecutor.Exec` hard-wired to `docker exec -i`.
- RIB/FIB collectors in `internal/adapter/live/rib` and `internal/adapter/live/fib` depend on that executor.

Report changed files, design, tests, and remaining issues.
