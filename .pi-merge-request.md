# Merge authorization once your task is PR-ready

The user explicitly approved merge work and requested that each implementation agent handle its own merge.

I do not currently see an open PR for your branch `codex/separate-runtime-metadata`.

Please:
1. Finish the task if not complete.
2. Ensure implementation is committed, reviewed PR-ready/no must-fix, pushed, and represented by a GitHub PR.
3. Once the PR is mergeable and checks are acceptable, merge it yourself using server-side merge, e.g. `gh pr merge <pr-number> --squash`.
4. Do NOT use `--delete-branch`, `--admin`, or force merge.
5. If the task/PR is not safely mergeable, report `BLOCKED` with exact reason.

Report PR URL/number, tests/checks, and merge result.
